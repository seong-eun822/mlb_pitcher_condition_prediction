# MLB 선발투수 컨디션 조기 예측 — Python 3.10+
# 설치: pip install -r requirements.txt

# ── 데이터 처리 ──────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0          # parquet I/O
duckdb>=0.10.0           # 수천만 행 투구 데이터 SQL 집계

# ── 모델링 ──────────────────────────────────────────────────
scikit-learn>=1.3.0
xgboost>=2.0.0           # 최종 채택 모델
lightgbm>=4.0.0
catboost>=1.2.0
optuna>=3.4.0            # 하이퍼파라미터 튜닝 (10번 노트북)

# ── 해석 / 통계 검증 ─────────────────────────────────────────
shap>=0.44.0             # feature 중요도
scipy>=1.11.0            # paired t-test (30 seed 검증)
joblib>=1.3.0            # 모델 저장/로드

# ── 시각화 ──────────────────────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.13.0

# ── 데이터 수집 ─────────────────────────────────────────────
pybaseball>=2.2.7        # Statcast 수집 (1_statcast/01)
requests>=2.31.0
beautifulsoup4>=4.12.0   # Baseball Savant play_id 크롤링 (2_video/02)

# ── 영상 파이프라인 (2_video/ — Colab GPU 권장) ───────────────
# 정형 모델링만 재현할 경우 아래 4개는 설치하지 않아도 됩니다.
opencv-python>=4.8.0
mediapipe>=0.10.0        # Pose — 14관절 좌표 추출
ultralytics>=8.0.0       # YOLOv8 — 투수 검출 (torch 자동 설치)
scenedetect>=0.6.2       # 9초 클립 컷 분할
