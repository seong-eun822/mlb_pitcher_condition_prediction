# 대용량 데이터 (statcast parquet, 좌표 CSV 380개 등) — 드라이브에만
data/*
!data/README.md

# 루트의 데이터/feature parquet (대용량, 드라이브에만 — starters_all은 267MB)
*.parquet

# 산출물 — 재생성 가능하므로 저장소에는 최소한만 둔다.
# README·실험기록이 인용하는 파일만 화이트리스트로 허용.
outputs/*
!outputs/experiments/
outputs/experiments/*
!outputs/experiments/experiment_results.md
!outputs/final/
outputs/final/*
!outputs/final/movement_gap_results.md
!outputs/final/best_params.json
!outputs/final/final_feature_cols_v4.csv
!outputs/figures/
outputs/figures/*
!outputs/figures/01_pred_vs_actual.png
!outputs/figures/02_shap_beeswarm.png
!outputs/figures/05_error_by_speed_type.png
!outputs/figures/shap_bio_comparison.png

# 내부 작업 문서 (Claude 참고용, 로컬 전용)
CLAUDE.md
00_로컬_드라이브_대조표.md
00_노트북_지도.md
MLB_pitcher_condition_prediction.md

# 기획/일정/발표 스크립트 (포폴 무관, 로컬 전용)
90_기획/

# 참고논문 PDF 원본 (54MB, 저작권) — 요약 md는 유지
docs/references/PDF/

# 미채택 탐색 기록 (초기 작업본·손가락검출 비교)
99_참고자료/

# 면접용 자료 (로컬 전용)
_면접자료/

# 실험 산출물 (버전별 모델/parquet — 대용량, 로컬 전용)
v3_output/
v4_output/
voutputs/

# 로컬 보관 (내부 작업 문서)
_보관/

# 개인 환경 설정 (데이터 경로 — 사람마다 다름)
.env

# 3D 포즈 실험 임시 산출물 (로컬 전용)
test3d/
test3d_*/
batch_slot*.zip

# 파이썬 캐시
__pycache__/
*.pyc
